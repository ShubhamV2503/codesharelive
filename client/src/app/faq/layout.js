export const metadata = {
    title: "FAQ",
    description: "Frequently asked questions about using codeshare 2.0, pricing, data privacy, and supported programming languages.",
};

export default function FaqLayout({ children }) {
    return <>{children}</>;
}
