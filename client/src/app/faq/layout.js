export const metadata = {
    title: "FAQ",
    description: "Frequently asked questions about using codesharelive, pricing, data privacy, and supported programming languages.",
};

export default function FaqLayout({ children }) {
    return <>{children}</>;
}
