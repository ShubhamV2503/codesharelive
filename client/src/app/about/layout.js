export const metadata = {
    title: "About",
    description: "Learn more about codesharelive, our mission, and the advanced technical features powering our platform.",
};

export default function AboutLayout({ children }) {
    return <>{children}</>;
}
