export const metadata = {
    title: "About",
    description: "Learn more about codeshare 2.0, our mission, and the advanced technical features powering our platform.",
};

export default function AboutLayout({ children }) {
    return <>{children}</>;
}
